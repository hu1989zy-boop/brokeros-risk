# MT4 Adapter

Placeholder for an isolated MT4 integration adapter.

No Manager API interface or behavior is defined here because the real SDK is
not currently part of the repository. Future implementation must use the real
SDK, must not leak MT4 types into the core domain, and must never directly
modify an external system database.
