-- Q-002 Phase 0.5 initial schema baseline.
--
-- Business tables are intentionally excluded. Flyway creates and maintains
-- flyway_schema_history; later schema changes must use new versioned migrations.
SELECT 1;
