# Unit and unrelated application-context tests do not connect to an external Kafka.
# Q-015's embedded-broker integration suite explicitly starts its real listener.
spring.kafka.listener.auto-startup=false
