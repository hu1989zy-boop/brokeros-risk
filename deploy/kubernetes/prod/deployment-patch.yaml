apiVersion: apps/v1
kind: Deployment
metadata:
  name: brokeros-risk-backend
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: brokeros-risk-backend
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1Gi
